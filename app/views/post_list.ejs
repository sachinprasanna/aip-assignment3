<%- include('partials/header') %>
<%- include('partials/menu') %>
<div class="listing container">
    <% if(locals.error) { %>
        <div class="alert alert-danger"><%= error %></div>
    <% } %>
    <% if(locals.success) { %>
        <div class="alert alert-success"><%= success %></div>
    <% } %>
        
    <form class="navbar-form searchForm" role="search">
      <div class="input-group add-on">
        <input class="form-control" placeholder="<%= __('search_word') %>" name="q" id="q" type="text" minlength="5" value="<%= locals.q || '' %>" />
        <div class="input-group-btn">
          <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
        </div>
      </div>
    </form>
    <!-- Post Container  -->
    <br/><br/>
    <% if(!posts.length) { %>
        <div class="alert alert-danger">No results found.</div>
    <% } %>
    <% for(var i=0; i < posts.length; i++) {%>
    <div class="text-center singlepost">
      <h3><%= posts[i].title %></h3>
      <p><em><%= __('by') %>: <%= posts[i].userFirstName %> <%= posts[i].userLastName %></em></p>
      <p><%= posts[i].content %></p>
    </div>
    <% } %>
    
</div>
<%- include('partials/footer') %>